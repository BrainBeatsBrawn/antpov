# Movies

This directory is a place to store temporary image files when making movies.

When antpov is invoked with the `-m` option, it will save screenshots at each recorded position of the ant into this directory.